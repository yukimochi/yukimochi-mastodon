version: 2.1

jobs:
  build-ruby:
    docker:
     - image: cimg/base:current
    resource_class: large
    steps:
      - checkout
      - setup_remote_docker
      - run: |
          TAG=circleci-<< pipeline.number >>
          echo $DOCKERHUB_PASSWORD | docker login -u $DOCKERHUB_USERNAME --password-stdin
          docker build -f Dockerfile -t $DOCKERHUB_USERNAME/mastodon:$TAG .
          docker push $DOCKERHUB_USERNAME/mastodon:$TAG
          docker run --name public_asset $DOCKERHUB_USERNAME/mastodon:$TAG tini --version
          docker cp public_asset:/opt/mastodon/public .
          docker build -f Dockerfile.proxy -t $DOCKERHUB_USERNAME/mastodon-proxy:$TAG .
          docker push $DOCKERHUB_USERNAME/mastodon-proxy:$TAG

  build-streaming:
    docker:
     - image: cimg/base:current
    steps:
      - checkout
      - setup_remote_docker
      - run: |
          TAG=circleci-<< pipeline.number >>
          echo $DOCKERHUB_PASSWORD | docker login -u $DOCKERHUB_USERNAME --password-stdin
          docker build -f streaming/Dockerfile -t $DOCKERHUB_USERNAME/mastodon-streaming:$TAG .
          docker push $DOCKERHUB_USERNAME/mastodon-streaming:$TAG

workflows:
  version: 2
  build:
    jobs:
      - build-ruby:
          filters:
            branches:
              only: production
      - build-streaming:
          filters:
            branches:
              only: production
